{% set name = "hydromt_wflow" %}
{% set version = "0.2.1" %}


package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/hydromt_wflow-{{ version }}.tar.gz
  sha256: cd70f28bbcfe590639ed36e74c07353da7fcee77f5832b9d69ccdbe5c794582a

build:
  number: 0
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv

requirements:
  host:
    - pip
    - flit
    - python >=3.8
  run:
    - dask
    - geopandas >=0.10
    - hydromt >=0.5.0
    - netcdf4
    - numpy
    - pandas
    - pyflwdir >=0.5.6
    - pygeos
    - pyproj
    - python >=3.8
    - rasterio
    - rioxarray
    - scipy
    - shapely
    - toml
    - xarray

test:
  imports:
    - hydromt_wflow
  commands:
    - pip check
  requires:
    - pip

about:
  home: https://pypi.org/project/hydromt_wflow/
  summary: hydroMT plugin for wflow models.
  dev_url: https://github.com/Deltares/hydromt_wflow
  license: GPL-3.0-or-later
  license_file: LICENSE

extra:
  recipe-maintainers:
    - DirkEilander
    - hboisgon
